# Desafio Iniciante

Crie uma classe `Car` que herda de `Vehicle`. Crie um módulo `Electric`. Faça com que `Electric` seja injetado de forma que, ao chamar um método que exista em ambos, o método do módulo `Electric` seja executado antes do método de `Car`.